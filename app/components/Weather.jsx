var React = require('react');

var Weather = React.createClass({
  render: function () {
    return (
      <h3>This is where the weather component will go.</h3>
    )
  }
});

module.exports = Weather;
